let readlineSync= require("readline-sync");
let n= readlineSync.questionInt("ingrese cantidad del arreglo");
let array= new Array(n);
function invertirRecursivo(n){
	
}
let indice;
for(indice=0;indice<n;i++){
	array=indice*10;
}